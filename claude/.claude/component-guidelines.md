# Claude Code Component Guidelines

When writing React/Vue/Svelte or HTML components:
- Structure UI hierarchically: Header, Command Bar / Action Bar, Bento Grid, Floating Feedback.
- Integrate Lucide icons for visual clarity on buttons and navigation items.
- Avoid inline styles where Tailwind classes can be used.
- Include transitions: `transition-all duration-200`.
