# GitHub Copilot UI/UX Instructions

When suggesting frontend code:
- Favor Tailwind CSS classes featuring glassmorphism (`backdrop-blur-xl`, `border-white/10`, `bg-slate-900/60`).
- Ensure all interactive elements include `:hover` and `:active` micro-transitions.
- Maintain dual typography: clean sans-serif for body + monospace for numerical data.
